"use client";

import "@copilotkit/react-ui/v2/styles.css";
import "@/styles/copilot-welcome.css";

/** Client-only CopilotKit styles (avoids pulling UI kit into the RSC server graph). */
export function CopilotStyles() {
  return null;
}
