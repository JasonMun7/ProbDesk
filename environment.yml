# Conda environment for Prob Desk (from repo root: conda env create -f environment.yml)
name: prob-desk
channels:
  - conda-forge
dependencies:
  - python>=3.10,<3.14
  - pip
  - pip:
      # Editable install + [ui] extra (ag-ui-adk, fastapi, uvicorn for agui_server)
      - -e ".[ui]"
